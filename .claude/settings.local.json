{
  "permissions": {
    "allow": [
      "Bash(pkill:*)",
      "Skill(update-config)",
      "Bash(python:*)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(python -c \"import sys,json; d=json.load(sys.stdin); print(d.get('tool_input',{}).get('file_path',''),end='')\"); [ -n \"$f\" ] || exit 0; echo \"$f\" | grep -qF '.claude' && exit 0; cd \"C:/Program2/T/04_manual\" || exit 0; git add \"$f\" 2>/dev/null; git diff --cached --quiet || (git commit -m \"auto: ファイルを更新\" && git push origin main); exit 0",
            "statusMessage": "自動コミット＆プッシュ中..."
          }
        ]
      }
    ]
  }
}
